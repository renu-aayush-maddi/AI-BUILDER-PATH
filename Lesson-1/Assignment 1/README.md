Questions

Write a Python function that takes a list of dictionaries representing user data, removes any dictionaries missing an 'email' key, and returns the sorted list by 'age'.Handle edge cases.

Write an optimized SQL query to join a 'Customers' table, an 'Orders' table, and a 'Products' table to find the top 5 customers who spent the most money in the last 30 days.

Write a Terraform script to deploy an AWS EC2 instance and an S3 bucket, ensuring the S3 bucket has versioning enabled and is private.